<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Route POC uniquement (SPEC_ANDROID §22 Lot 0, CLAUDE.md : pas de faux comportement de
        production). Ce fichier entier est absent de la variante release et supprimé avec le
        reste de la route POC à l'étape 21.

        La section <queries> qui vivait ici jusqu'à l'étape 13 a été déplacée dans le manifeste de
        `:core:system`, avec le code qui en dépend (`PackageManagerPackageQuery`). Le manifeste
        fusionné la porte dans tous les variants : `PocViewModel.resolveLabel` continue de
        résoudre les libellés des applications tierces.
    -->
    <application>
        <activity
            android:name=".poc.PocPairingActivity"
            android:exported="false"
            android:theme="@style/Theme.Niumi" />
    </application>

</manifest>

// Fichier de repère : matérialise le package avant que l'étape 3 (alarme, audio, sonnerie)
// n'y ajoute du code réel. Un fichier réduit au seul mot-clé `package` est traité comme vide
// par ktlint (standard:no-empty-file) ; l'annotation ci-dessous documente et couvre ce cas.
@file:Suppress("ktlint:standard:no-empty-file")

package com.niumi.system
